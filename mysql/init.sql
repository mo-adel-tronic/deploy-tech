-- This is an optional file for initializing your MySQL database.
-- You can add your SQL commands here to create tables, insert initial data, etc.
-- This script will be executed when the MySQL container starts for the first time.

-- Example:
-- CREATE DATABASE IF NOT EXISTS my_database;
-- USE my_database;
-- CREATE TABLE IF NOT EXISTS users (
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     name VARCHAR(255) NOT NULL,
--     email VARCHAR(255) UNIQUE NOT NULL
-- );
-- INSERT INTO users (name, email) VALUES (
--     'Test User', 'test@example.com'
-- );

